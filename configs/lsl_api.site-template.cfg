; Copy to lsl_api.site.cfg and fill organizer-approved IPs/session on BOTH PCs.
; This file is an example, not a verified event-network configuration.
[lab]
KnownPeers = {}
; SessionID must match on the EEG sender and official game receiver.
; SessionID = REPLACE_WITH_ORGANIZER_ASSIGNED_TEAM_SESSION
